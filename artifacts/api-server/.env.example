# Copy to .env for local (non-Replit) dev.
PORT=8080
SESSION_SECRET=change-me
RAWEDOG_CREDITS_ENFORCED=false

# Optional — only needed to exercise the corresponding features locally:
# XAI_API_KEY=
# THEIRSTACK_API_KEY=
